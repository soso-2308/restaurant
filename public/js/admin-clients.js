// Fonctions pour la gestion des clients (ex: confirmation de suppression, etc.)
document.addEventListener('DOMContentLoaded', function() {
    // Exemple : si vous ajoutez une fonction de suppression de client plus tard
    console.log('Admin clients JS chargé');
});